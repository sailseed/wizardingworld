<!DOCTYPE html>
<html>
<head>
    <title>Editar Gasto</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body class="container mt-4">
    <div class="card shadow">
        <div class="card-header bg-dark text-white">
            <h2 class="mb-0">Editar Gasto <%= item.g_code %></h2>
        </div>
        <div class="card-body">
            <form action="/gastos/edit/<%= item.id %>" method="POST">
                <div class="mb-3">
                    <label class="form-label">Descripción</label>
                    <input type="text" name="description" value="<%= item.description %>" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Precio ($)</label>
                    <input type="number" step="0.01" name="price" value="<%= item.price %>" class="form-control" required>
                </div>


                <% if (item.receipt_img) { %>
    <div class="mb-3 text-center">
        <p>Recibo actual:</p>
        <img src="/uploads/<%= item.receipt_img %>" class="img-thumbnail" style="max-height: 200px;">
    </div>
<% } %>


                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary btn-lg">Actualizar Gasto</button>
                </div>

            </form>


            <hr class="my-4">

            <form action="/gastos/delete/<%= item.id %>" method="POST" onsubmit="return confirm('¿Eliminar este gasto permanentemente?')">
                <div class="d-grid">
                    <button type="submit" class="btn btn-outline-danger">Eliminar Gasto</button>
                </div>
            </form>

            <div class="text-center mt-3">
                <a href="/gastos" class="btn btn-link text-muted">Cancelar y Volver</a>
            </div>
        </div>
    </div>
</body>
</html>
