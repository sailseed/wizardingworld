<!DOCTYPE html>
<html>
<head>
    <title>Historial de Gastos</title>
    <!-- This link is what makes it look like Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        .table-v-align td { vertical-align: middle; }
    </style>
</head>
<body class="bg-light">
    <div class="container mt-4">
        <div class="card shadow-sm">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h1 class="h2">Historial de Gastos</h1>
                    <div>
                        <a href="/" class="btn btn-outline-secondary me-2">Dashboard</a>
                        <a href="/gastos/agregar" class="btn btn-danger btn-lg">+ Agregar Gasto</a>
                    </div>
                </div>

                <div class="table-responsive">
                    <table class="table table-hover border table-v-align">
                        <thead class="table-dark">
                            <tr>
                                <th>Acción</th>
                                <th>ID</th>
                                <th>Fecha y Hora</th>
                                <th>Descripción</th>
                                <th>Precio</th>
                                <th>Session#</th>
                                <th>Recibo</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% items.forEach(item => { %>
                                <tr>
                                    <td>
                                        <a href="/gastos/edit/<%= item.id %>" class="btn btn-sm btn-outline-primary">Editar</a>
                                    </td>
                                    <td><span class="badge bg-secondary"><%= item.g_code %></span></td>
                                    <td class="text-muted small"><%= item.timestamp %></td>
                                    <td><strong><%= item.description %></strong></td>
                                    <td><strong>$<%= item.price %></td>
                                    <td><span class="badge bg-light text-dark border"><%= item.session_id %></span></td>
                                    <td>
                                        <% if (item.receipt_img) { %>
                                            <a href="/uploads/<%= item.receipt_img %>" target="_blank" class="btn btn-sm btn-info text-white">
                                                Ver 📷
                                            </a>
                                        <% } else { %>
                                            <span class="text-muted small">Sin foto</span>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>

                <% if (items.length === 0) { %>
                    <div class="text-center py-5">
                        <p class="text-muted">No hay gastos registrados aún.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
